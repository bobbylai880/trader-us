---
name: project_conventions
type: configuration
description: Amazon Business Analysis System 项目规范与约定
version: 1.1.0
last_updated: 2026-01-19
---

# 🏗️ Project Conventions

本文档定义 Amazon Business Analysis System 的强制规范，所有 AI agents 和人类贡献者必须遵守。

---

## 📁 目录结构规范（强制）

### OpenCode 标准结构

```
.opencode/
├── agent/          # ✅ 代理定义（单数形式）
├── skill/          # ✅ 技能SOP（单数形式）
├── tool/           # ✅ 工具与资源（单数形式）
├── conventions.md  # ✅ 本文件（项目规范）
└── package.json    # ✅ OpenCode插件依赖
```

**命名约定**：
- ✅ **必须使用单数**：`agent/`, `skill/`, `tool/`（不是 `agents/`, `skills/`, `tools/`）
- ❌ **禁止使用复数**：这是 OpenCode 官方标准
- ❌ **禁止在根目录创建 `tools/` 或类似目录**：所有资源必须在 `.opencode/` 下

### 工具分类规范

`.opencode/tool/` 目录存放所有认知资源：

| 分类 | 命名前缀 | 示例 |
|------|---------|------|
| 数据库Schema | `db_*` | `db_schema.md` |
| 分析方法 | `method_*` | `method_5_whys.md`, `method_fishbone.md`, `method_pareto.md`, `method_swot.md` |
| 算法规则 | `rule_algo_*` | `rule_algo_a9.md`, `rule_algo_cosmo.md` |
| 策略框架 | `strategy_*` | `strategy_plc.md` |

---

## 🔧 OpenCode 官方配置规范（强制）

### opencode.json 配置规范

**文件位置**：项目根目录 `opencode.json`

**标准格式**：
```json
{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "agent-name": {
      "description": "代理描述（必需）",
      "mode": "subagent",
      "file": ".opencode/agent/agent-name.md"
    }
  }
}
```

**必需字段**：
| 字段 | 类型 | 说明 |
|------|------|------|
| `description` | string | ✅ 必需。代理功能描述，用于 Agent 发现 |
| `mode` | string | ✅ 必需。`primary` 或 `subagent` |
| `file` | string | ✅ 必需（使用外部文件时）。指向 .md 定义文件 |

**可选字段**：
| 字段 | 类型 | 说明 |
|------|------|------|
| `model` | string | 可选。覆盖默认模型 |
| `prompt` | string | 可选。内联系统提示 |
| `tools` | object | 可选。工具权限配置 |
| `temperature` | number | 可选。温度参数 |
| `color` | string | 可选。TUI 显示颜色 |

---

### Agent Markdown 文件规范

**文件位置**：`.opencode/agent/{agent-name}.md`

**⚠️ 关键规则**：
- ❌ **禁止在 frontmatter 中使用 `name` 字段**：Agent 名称由**文件名**决定
- ✅ Agent 名称 = 文件名（不含 `.md` 后缀）

**标准 Frontmatter 格式**：
```yaml
---
description: 代理功能描述（必需）
mode: subagent
model: provider/model-name
temperature: 0.1
tools:
  tool-name: true
  another-tool: false
---

系统提示内容...
```

**OpenCode 官方认可的 Frontmatter 字段**：
| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `description` | string | ✅ 必需 | 代理功能描述 |
| `mode` | string | ✅ 必需 | `primary`, `subagent`, 或 `all` |
| `model` | string | 可选 | 完整模型ID (provider/model-name) |
| `temperature` | number | 可选 | 温度参数 (0.0-1.0) |
| `tools` | object | 可选 | 工具权限 (tool-name: true/false) |

**项目扩展字段**（OpenCode 会忽略，仅供项目内部使用）：
| 字段 | 类型 | 说明 |
|------|------|------|
| `cognitive_tools` | array | 认知工具引用列表 |
| `subordinates` | array | 下属 Agent 列表（仅 Director） |

**❌ 禁止的字段**：
| 字段 | 原因 |
|------|------|
| `name` | Agent 名称由文件名决定，不需要此字段 |

---

## 🤖 Agent 规范

### Agent 文件结构

**文件位置**：`.opencode/agent/*.md`

**Frontmatter 示例**（符合 OpenCode 官方规范）：
```yaml
---
description: 亚马逊运营总监。负责语义路由、任务分发、数据搜集与深度决策。
mode: subagent
model: google/antigravity-gemini-3-pro
tools:
  hyy-bi-mcp: true
cognitive_tools:
  - rule_algo_a9
  - method_5_whys
subordinates:
  - product_manager
  - finance_analyst
---
```

**模型ID规范**：
- ✅ **必须使用完整ID**：`google/antigravity-gemini-3-pro`（包含provider前缀）
- ✅ **必须与OpenCode配置匹配**：检查 `~/.config/opencode/opencode.json` 中定义的模型ID
- ❌ **禁止使用不存在的ID**：如 `google/gemini-3-pro`（未在配置中定义）

**推荐模型配置**：
| Agent类型 | 推荐模型 | 理由 |
|----------|---------|------|
| Director | `google/antigravity-gemini-3-pro` | 需要强推理能力 |
| Analysts | `google/antigravity-gemini-3-flash` | 快速响应，成本低 |

**必需章节**：
1. `# 🤖 Identity` 或 `# 🧠 Identity & Role` - 代理身份与职责
2. `# 🌍 Global Context` - 可用资源与约束
3. `# ⚡ Prime Directive` - 核心行为规则

### 权限矩阵（强制）

| Agent | get_child_asins | amz_asin_sales_performance | query_database |
|-------|----------------|---------------------------|----------------|
| **director** | ❌ 禁止 | ❌ 禁止 | ❌ 禁止 |
| **product_manager** | ✅ 允许 | ❌ 禁止 | ❌ 禁止 |
| **finance_analyst** | ❌ 禁止 | ✅ 允许 | ❌ 禁止 |

**原则**：
- ❌ **Director 禁止直接访问数据**：必须通过派单给下属获取
- ❌ **禁止所有 agent 使用 `query_database`**：只能使用封装好的 MCP 工具
- ✅ **最小权限原则**：每个 agent 只能访问其职责范围内的工具

---

## 📋 Skill 规范

### Skill 文件结构

**文件位置**：`.opencode/skill/{domain}/{skill_name}.md`

**目录组织**：
```
.opencode/skill/
├── product/       # 产品相关技能
│   └── variant_query.md
└── finance/       # 财务相关技能
    └── sales_analysis.md
```

**必需 Frontmatter**：
```yaml
---
name: skill_name           # 必需：技能名称（snake_case）
description: 功能描述      # 必需：技能用途
适用场景: 使用场景        # 必需：中文说明
触发关键词: 关键词列表    # 必需：触发词（逗号分隔）
---
```

**标准 SOP 结构（5步）**：
1. **Step 1: 参数提取与验证** - 解析用户输入
2. **Step 2: MCP 工具调用** - 执行数据查询
3. **Step 3: 数据解析与分析** - 处理返回结果
4. **Step 4: JSON 报告生成** - 结构化输出
5. **Step 5: 自然语言摘要** - 用户友好的总结

**示例要求**：
- 必须包含至少 3 个完整示例：成功案例、空结果、错误处理

---

## 🛠️ Tool 规范

### Tool 文件结构

**文件位置**：`.opencode/tool/*.md`

**必需 Frontmatter**：
```yaml
---
name: tool_name           # 必需：工具名称
type: cognitive_tool      # 必需：工具类型（cognitive_tool 或 reference）
description: 工具描述     # 必需：用途说明
适用场景: 使用场景        # 可选：中文说明
触发关键词: 关键词列表    # 可选：触发词（逗号分隔）
---
```

**工具类型说明**：
| type | 用途 |
|------|------|
| `cognitive_tool` | 分析方法、算法规则、策略框架 |
| `reference` | 参考文档、数据库 Schema |

**内容要求**：
- 必须包含方法论说明、使用步骤、示例场景
- 数据库相关工具必须包含完整的 SQL 示例
- 分析方法必须包含输出模板

---

## 📝 文档规范

### Markdown 样式

**Emoji 使用规范**：
```markdown
# 🤖 Identity      # Agent身份
# 🌍 Global Context # 全局上下文
# ⚡ Prime Directive # 核心指令
# 🎯 Goals          # 目标
# 🚫 Constraints    # 约束
# 📚 References     # 参考
```

**代码块**：
- ✅ **必须指定语言**：` ```bash `, ` ```json `, ` ```sql `
- ❌ **禁止使用单引号**：使用三个反引号

**表格**：
- 必须使用 Markdown 表格格式
- 标题行必须左对齐

### 文档更新规则

当修改目录结构或添加新组件时，必须同步更新以下文件：
1. `README.md` - 项目结构章节
2. `AGENTS.md` - 开发指南
3. 相关 Agent 文件 - 技能路径引用
4. 本文件 (`conventions.md`) - 规范定义

---

## 🔧 命名规范

### 文件命名

| 类型 | 格式 | 示例 |
|------|------|------|
| Agent | `{name}.md` | `director.md`, `finance_analyst.md` |
| Skill | `{function}.md` | `variant_query.md`, `sales_analysis.md` |
| Tool | `{category}_{name}.md` | `method_5_whys.md`, `rule_algo_a9.md` |
| 数据库Schema | `db_{entity}.md` | `db_schema.md` |

### 变量命名

| 层级 | 格式 | 示例 |
|------|------|------|
| Agent 名称 | `snake_case` | `product_manager`, `finance_analyst` |
| Skill 名称 | `snake_case` | `variant_query`, `sales_analysis` |
| MCP 工具 | `snake_case` | `get_child_asins`, `amz_asin_sales_performance` |
| 数据库字段 | `camelCase` | `parentAsin`, `itemName` |

---

## 🔒 安全与权限

### MCP 工具使用规范

**禁止事项**：
- ❌ **禁止使用 `query_database`**：所有 SQL 查询必须通过封装的 MCP 工具
- ❌ **禁止跨权限访问**：Agent 不能使用未在其 frontmatter 中声明的工具
- ❌ **禁止修改数据**：所有 MCP 工具只能读取，不能写入/更新/删除

**安全检查清单**：
- [ ] Agent 是否声明了所需工具？
- [ ] 工具访问是否符合权限矩阵？
- [ ] 是否避免了 SQL 注入风险？（使用参数化查询）

---

## 📦 版本控制规范

### Git 提交规范

**Commit Message 格式**：
```
type: description

详细说明（可选）
```

**Type 类型**：
- `feat` - 新功能
- `fix` - Bug修复
- `docs` - 文档更新
- `refactor` - 重构（不改变外部行为）
- `chore` - 构建/工具变更
- `config` - 配置变更

**示例**：
```bash
git commit -m "feat: add profit analysis skill"
git commit -m "docs: update agent directory structure"
git commit -m "config: update opencode.json with full agent config"
```

### 分支规范

- `main` - 主分支（稳定版本）
- `feature/*` - 功能分支
- `fix/*` - 修复分支

---

## 🚀 扩展指南

### 添加新 Agent

1. 创建 `.opencode/agent/{name}.md`
2. 填写必需的 frontmatter（**不要包含 `name` 字段**）
3. 更新 `opencode.json` 添加 agent 配置（包含 `description`, `mode`, `file`）
4. 在 `.mcp.json` 中配置工具权限
5. 更新 `README.md` 和本文件

### 添加新 Skill

1. 创建 `.opencode/skill/{domain}/{name}.md`
2. 定义完整的 5 步 SOP
3. 提供 3 个示例（成功/空结果/错误）
4. 在对应 Agent 中引用

### 添加新 Tool

1. 创建 `.opencode/tool/{category}_{name}.md`
2. 编写方法论、步骤、示例
3. 在 Agent 的 `cognitive_tools` 中引用

---

## ✅ 验证检查清单

在提交代码前，确保：

- [ ] 所有目录遵循单数命名（`agent/`, `skill/`, `tool/`）
- [ ] 所有 Agent 文件 frontmatter **不包含 `name` 字段**
- [ ] `opencode.json` 中每个 agent 都有 `description` 和 `mode`
- [ ] 所有 Skill 文件包含 5 步 SOP 和 3 个示例
- [ ] 所有 Tool 文件包含 `name`, `type`, `description`
- [ ] 文档引用路径正确（无 `agents/`, `skills/`, `tools/`）
- [ ] Commit message 符合格式规范
- [ ] 权限矩阵遵循最小权限原则

---

## 📚 参考文档

- **OpenCode 官方文档**: https://opencode.ai/docs
- **OpenCode Agent 配置**: https://opencode.ai/docs/agents
- **OpenCode 配置规范**: https://opencode.ai/docs/config
- **项目架构**: `README.md`
- **Agent 开发指南**: `AGENTS.md`
- **MCP 配置**: `.mcp.json`
- **数据库Schema**: `.opencode/tool/db_schema.md`

---

**最后更新**: 2026-01-19  
**维护者**: Project Team  
**版本**: 1.1.0  
**状态**: ✅ 强制执行
